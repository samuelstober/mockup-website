import project from './project'
import imageEntry from './imageEntry'
import about from './about'
import siteSettings from './siteSettings'

export const schema = {
  types: [project, imageEntry, about, siteSettings],
}
